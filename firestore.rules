rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /projects/{projectId} {
      allow read: if true; // tout le monde peut lire
      allow write: if request.auth.token.admin == true; // seul un admin peut écrire
    }
  }
}



